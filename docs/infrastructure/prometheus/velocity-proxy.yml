global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: festas-velocity-proxy
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - 127.0.0.1:9108
        labels:
          network: festas
          component: velocity-proxy
