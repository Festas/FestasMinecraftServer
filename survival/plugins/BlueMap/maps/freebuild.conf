# Disabled on purpose: the live survival server no longer has this world.
# Keep the file in a non-rendering state because deploys are additive.
# Hidden from the web UI via webapp JS; BlueMap itself would still list this map while world: is omitted.
# world: "freebuild"
dimension: "minecraft:overworld"
name: "Freebuild"
sorting: 30
storage: "file"
sky-color: "#7dabff"
void-color: "#000000"
remove-caves-below-y: 55
